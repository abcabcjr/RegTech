# Vulnerable SNMP configuration

# Vulnerable: Use default community strings
rocommunity public default
rwcommunity private default

# Vulnerable: Allow access from any host
rocommunity public 0.0.0.0/0
rwcommunity private 0.0.0.0/0

# System information (information disclosure)
syslocation "Vulnerable Server Room, Building A"
syscontact "admin@vulnerable.local"
sysname "vulnerable-snmp-server"

# Vulnerable: Enable all MIB modules
view all included .1 80

# Vulnerable: Allow full access
access notConfigGroup "" any noauth exact all none none

# Enable AgentX (potential security risk)
master agentx
agentXSocket tcp:localhost:705

# Logging
logTimestamp yes

# Vulnerable: Expose system information
extend test1 /bin/echo "Hello World"
extend-sh test2 echo "Shell command test"

# Vulnerable: Allow disk and process monitoring
includeAllDisks 10%
proc sshd
proc httpd 5 10

# Load average monitoring
load 12 14 14

# Vulnerable: Default trap configuration
trap2sink localhost public
informsink localhost public

# Vulnerable: Enable SNMP v1 and v2c (weak protocols)
# SNMPv3 users would be more secure, but we're intentionally vulnerable
